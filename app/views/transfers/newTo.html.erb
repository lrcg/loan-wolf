<div class="row">
	<div class="span5">
		<%= form_for(@transfer, :as => 'Transfer', :url => transfers_create_path, :method => :post, :html => { :class => 'form-horizontal well' }) do |f| %>

		  <fieldset>	
		  		<legend>New Loan To...</legend>
		  		<div class="control-group">
		  			<%= f.label :debtor_user_id, :class => 'control-label' %>
					<div class="controls">
						<%= f.select :debtor_user_id, options_from_collection_for_select(@users, :id, :initials) %>
			  		</div>
			  	</div>
			  	<div class="control-group">
			  		<%= f.label :amount, :class => 'control-label' %>
					<div class="controls">
						<%= f.text_field :amount %>
			  		</div>
				</div>
				<div class="form-actions"><%= f.submit "Loan it!", :class => 'btn' %></div>
			</fieldset>
		<% end %>
	</div>
</div>
